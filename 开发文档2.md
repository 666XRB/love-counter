
---

# 📂 项目全称：爱计数 (Love Counter) 终极开发文档

## 1. 项目愿景与 UI 规范

* **目标**：一个私密的、具备云同步功能的静态单页应用（SPA），用于记录亲密行为频率与质量。
* **视觉风格**：**Windows 11 Mica (亚克力) 风格**。
* **背景**：淡灰色或极简磨砂玻璃背景。
* **容器**：高透明度白色背景 (`bg-white/70`)、大圆角 (`rounded-3xl`)、深度模糊 (`backdrop-blur-2xl`)、极细边框 (`border-white/40`)。
* **动效**：按钮悬浮有微缩放 (`scale-105`)，点击有触感反馈。



## 2. 技术架构 (Stack)

* **前端框架**：React + Vite
* **样式方案**：Tailwind CSS (响应式布局，支持移动端)
* **数据库 (BaaS)**：LeanCloud (国内线路，支持多设备实时同步)
* **工具库**：`date-fns` (日期处理), `lucide-react` (图标), `html2canvas` + `jspdf` (PDF 导出)

---

## 3. 核心功能实现细节

### A. 数据库配置 (`/src/api/leancloud.js`)

使用提供的 Credentials 初始化：

```javascript
import AV from 'leancloud-storage';
AV.init({
  appId: "fXnYOxrBJqYINPMyp49sG3Sh-gzGzoHsz",
  appKey: "4VGgTvlU2vWsdjK4QxM9ArcB",
  serverURL: "https://fxnyoxrb.lc-cn-n1-shared.com"
});
export const LoveRecord = AV.Object.extend('LoveRecord');

```

### B. 年度热力图算法

* **逻辑**：获取当前日期向前推 365 天的所有天数。
* **展示**：仿 GitHub 贡献图。
* 0 次：`#EBEDF0` (淡灰)
* 1 次：`#FFC2D1` (浅粉)
* 2 次：`#FB6F92` (亮粉)
* 3 次+：`#C9184A` (深红 + 外发光)


* **交互**：点击热力图中的某个格子，弹窗显示该天的详细记录（时间、评分）。

### C. 数据统计与同步

* **实时同步**：每 30 秒执行一次 `query.find()`，确保你记录后，女朋友的页面无需刷新即可更新。
* **统计维度**：累计次数、本月频率、平均幸福指数（星级平均值）、最长连续记录。

---

## 4. 关键代码模块

### 导出 PDF 逻辑

```javascript
const exportReport = async () => {
  const element = document.getElementById('report-template'); // 渲染隐藏的精美模版
  const canvas = await html2canvas(element, { scale: 3 });
  const imgData = canvas.toDataURL('image/png');
  const pdf = new jsPDF('p', 'mm', 'a4');
  pdf.addImage(imgData, 'PNG', 0, 0, 210, 297);
  pdf.save('Our_Sweet_Report.pdf');
};

```

### 移动端优化

* 导航栏固定在底部（类似 App）方便单手点击记录。
* 热力图支持水平滑动查看。

---

## 5. 部署方案 (GitHub Pages)

1. **仓库设置**：在 GitHub 新建仓库 `love-counter`。
2. **环境变量**：虽然是静态页面，但建议将 AppKey 等信息放在 `.env` 文件中，通过 Vite 加载。
3. **CI/CD**：使用 GitHub Actions 自动部署。创建 `.github/workflows/deploy.yml`:
```yaml
name: Deploy to GitHub Pages
on:
  push:
    branches: [main]
jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Install and Build
        run: |
          npm install
          npm run build
      - name: Deploy
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          folder: dist

```



---

## 6. 开发者提醒 (防坑指南)

1. **LeanCloud 域名白名单**：部署后，必须在 LeanCloud 后台将 `https://你的用户名.github.io` 加入“Web 安全域名”，否则无法写入数据。
2. **文件服务初始化**：你在 LeanCloud 后台看到的“文件服务尚未初始化”错误，点击“初始化”即可，这不影响记录次数，只影响你未来是否想上传照片记录。
3. **PDF 字体**：导出的 PDF 若包含中文，需要配置 jsPDF 字体库或使用 `html2canvas` 转图片，建议直接转图片形式嵌入 PDF 以免乱码。

---

